apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - nginx

namespace: beta

commonLabels:
  env: beta

images:
  - name: nginx
    newTag: "1.27.0"

replicas:
  - name: nginx
    count: 1