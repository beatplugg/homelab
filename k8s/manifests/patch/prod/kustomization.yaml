apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - nginx

namespace: prod

commonLabels:
  env: prod

images:
  - name: nginx
    newTag: "1.27.0"

replicas:
  - name: nginx
    count: 2