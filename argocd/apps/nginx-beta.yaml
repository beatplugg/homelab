apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nginx-beta
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/<your-user>/<your-repo>.git
    targetRevision: HEAD
    path: k8s/apps/patch/beta/nginx
  destination:
    server: https://kubernetes.default.svc
    namespace: beta
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true